import React from "react";
import Contact from "@/app/pages/landing/Contact";

const ContactAdminPage = () => {
  return React.createElement(Contact, { embedded: true });
};

export default ContactAdminPage;
